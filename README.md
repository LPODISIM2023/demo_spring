## Lanciare l'application
Potete importarlo all'interno di eclipse (come existing maven project) e lanciare la classe ```DemoApplication```, oppure più semplicemente con il comando maven dalla root del repository
```mvn spring-boot:run``` .
